bases:
- ../with-kubedb

resources:
- pc-ingress.yaml

patchesStrategicMerge:
- patch-use-mail-variables.yaml

configMapGenerator:
- name: permanent.cloud
  files:
  - pc-kustomize-variables.yaml
- name: parameters
  behavior: merge
  literals:
  - INGRESS_HTTP_PROTOCOL=https
  - INGRESS_WS_PROTOCOL=wss
